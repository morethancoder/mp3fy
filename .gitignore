node_modules/
.svelte-kit/
build/
dist/
.DS_Store
.env
.env.*
!.env.example
.playwright/
.playwright-mcp/
vite.config.ts.timestamp-*

# Tauri
src-tauri/target/
src-tauri/gen/schemas/
src-tauri/gen/apple/
# gen/android IS committed: it carries the engine wiring (youtubedl-android
# deps, the yt-dlp plugin, the share intent filters), which `tauri android
# init` would otherwise regenerate away.
